{
  "name": "desert-safari-monorepo",
  "version": "0.0.0",
  "packageManager": "npm@10.2.3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "postinstall": "turbo run generate"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}